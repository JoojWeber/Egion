package tela;

import com.badlogic.gdx.Gdx;

public class rascunhos {

    /*
    angulo0 = (float) Math.toDegrees(Math.acos(disX / Math.hypot(disX,disY)));
    disX = (Gdx.input.getX() - pac.getX() - pac.getWidth() / 2);
    disY = (Gdx.graphics.getHeight() - Gdx.input.getY() - pac.getY() - pac.getHeight() / 2);
    angulo1 = (float) Math.toDegrees(Math.acos(disX / Math.hypot(disX,disY))) - angulo0;
    pac.rotate((float) disY>0?angulo1:-angulo1);
    */
}
